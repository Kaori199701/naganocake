<h1>商品詳細</h1>

  <%= image_tag @item.get_image %>

<p>商品名</p>
  <%= @item.item_name %>

<p>商品説明</p>
  <%= @item.item_introduction %>

<p>ジャンル</p>
  <%= @item.genre.genres_name %>

<p>税込価格(税抜価格)　　　円</p>
  <%= @item.price.to_s(:delimited) %>(<%= @item.add_tax_price.to_s(:dalimited) %>) 円

<p>販売ステータス</p>
  <% if @item.is_active %>
      販売中
  <% elsif %>
      販売停止中
  <% end %>


<p><%= link_to "編集する", edit_admins_item_path(@item.id), class:"btn btn-sm btn-success" %></p>
